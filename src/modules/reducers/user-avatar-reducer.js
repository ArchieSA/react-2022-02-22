const initialState = {
    name: 'Alex',
    cash: '1000$'
}

export const userAvatarReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}